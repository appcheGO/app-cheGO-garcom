import { Box, Container, Typography } from '@mui/material';

export const Login = () => {
  return (
    <Container sx={{ backgroundColor: '#fff' }}>
      <Box>
        <Typography sx={{ color: '#f46c26' }}>
          CheGO
        </Typography>
      </Box>
    </Container>
  );
};
